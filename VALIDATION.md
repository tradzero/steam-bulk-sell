# 验证记录 — 2026-09-08

## 已完成

- TypeScript strict 构建通过。
- 31 项自动化测试通过，使用 Node test runner 和 jsdom。
- 开发依赖安装禁用生命周期脚本；当次 npm audit 返回 0 个已知漏洞。生产扩展没有 npm 运行时依赖。
- 在指定 Chrome 实例中，通过扩展实际 UI 的本地演示适配器完成：加载库存、选 2 种/15 件、获取参考价、应用定价、核对、勾选确认、完成模拟交接。
- 演示核对值为到手 108.80 CNY、买方支付 124.95 CNY；两项分别为 10 × 9.96 / 11.44 和 5 × 1.84 / 2.11。
- 上述浏览器流程没有控制台错误。没有真实出售请求。
- 在 1024px 宽度检查最终工作台布局；另验证超出持有量时核对按钮禁用、筛选外已选物品提示、缺失行情时仅勾选普通确认仍不能交接。检查后已恢复浏览器尺寸。
- 读取了真实 Steam 库存页和原生 multisell 的公开界面，并检查官方库存/费用/批量出售脚本。
- 将整数费用与逆向价格计算，同当日下载的 Steam `economy_common.js` 在隔离 Node VM 内进行 34,296 组比较，全部一致。覆盖 minimum 1/100、increment 1/100、publisher fee 0/0.10/0.15，以及输入区间 1–9999（步长 7）；Steam fee 为 0.05。

官方脚本解压后的 SHA-256：

```text
84429d06a9ef8c250ac567f9fedc9fb155d23ceb7442c92a19b815840c102784  economy_common.js
3c34054c2de58d2afa4fa3c4f811b5d9014ed68e8ea64aef60f19078d03e0f39  market_multisell.js
```

原始源码在本次调研工作目录中用于只读对照，没有将整份 Valve 脚本打包进插件。

## 自动化覆盖

- 来源/页面限制、手动价格和本地化单价解析、多个金额混在同一字符串时拒绝解析。
- 最小费用、货币步长、正反向价格、特殊字符市场名称的 URL 编码。
- 全量分页、重复资产、总量变化、丢失分页、游标不前进、非同质/不可上架物品排除。
- 超额数量、无效单价、重复清单、批次上限、过期/缺失/明显偏低的参考价提示。
- 非扩展消息、会话伪造、不同工作台、不同账户、不同游戏上下文。
- 不可修改的核对单、确认门槛、核对过期、参考价在核对期间过期、重复交接阻止。
- 429 不重试及冷却、草稿变化使核对单失效、来源标签页关闭时清除会话、重复工具栏点击复用工作台。
- 原生 DOM 夹具：正确填充、加载未完成保持零数量、库存减少、同名非同质资产、货币/费用变化、部分填充后取整失败、原生校验失败、错误账户、过期、正在出售、缺少输入框。
- 对原生模拟提交按钮和网络请求计数，填充过程产生 0 次提交，协议复选框未改变。
- 发布产物权限/CSP，以及出售请求、秘密读取、远程代码、HTML 注入危险入口的静态回归检查。

## 尚未完成的真实环境验收

本次未将扩展安装到用户 Chrome，未使用扩展读取真实库存 API，也未在真实原生页面中运行填充函数，更未点击创建上架。

因此不能将本地演示和夹具测试等同于 Chrome 扩展权限授予、真实库存分页、所有游戏/货币或 Steam 实际成交兼容性已经验证。

安装后建议先选少量低价值的同质物品，逐项核对真实库存数量、买方金额、到手金额、原生填充值。由用户决定是否完成真实上架。遇到结构、费用或价格不匹配应停止并记录具体提示。

未验证：跨全部游戏的费用规则、所有币种和语言、Steam 限流阈值、大规模真实库存、真实提交后的部分成功、邮件/手机确认及成交结果。首版对无法确认的数据会停止，而不会猜测执行。

## v0.1.1 价格口径与手续费

- 新增两种输入口径的金额往返、最低费用和货币步长测试，以及实际 UI 脚本的 DOM 交互回归；共 34 项测试通过。
- DOM 回归覆盖筛选外价格同步切换、无效输入阻止整个切换、取整提示、草稿与核对请求保持到手金额。
- Chrome 本地演示确认到手 9.96 → 买方 11.44、每件手续费 1.48；两件核对单显示到手 19.92、手续费 2.96、买方支付 22.88。
- 本次验证使用演示库存，没有提交真实 Steam 上架。

## v0.1.2 布局与原生交接修正

- 修正所有表格首列共享 50px 宽度的问题；库存表物品列 25%，核对表物品列 28%，两张表分别定义 colgroup。在 1920px Chrome 演示中，核对表名称列实测 370px，数量 92px，其他金额列 132–185px，提示 224px。
- 已确认 executeScript 返回 null 时旧代码会继续读取 state；新增空值与结构校验，原生函数异常改为可序列化的阶段/原因返回，避免错误被二次异常覆盖。
- 等待原生页面初始化、库存完整和输入框就绪。通过 Steam GetTotalWithFees 校验后直接填入两侧金额，再由 UpdateOrderTotal 校验合计；不再调用会读取无关行资产描述的 PriceRecvChanged。
- 39 项测试通过，包括初始化等待、格式化失败、缺失行资产联动函数、空/错误结果和清理过程异常。演示核对 20 件、到手 52.19、买方 60.00 的合计与用户截图一致。
- 用户报错的原标签页已不在当前标签列表，无法恢复当时被覆盖的首个异常。已只读查看重新打开的真实原生表单结构与脚本，未提交上架。浏览器策略禁止访问 chrome://extensions/，因此未能自动加载修复版做真实扩展填表验证；不能据此认定原始填表问题已在真实环境复测通过。

## v0.1.3 原生库存描述缺失的确定性复现与修复

- Steam economy_v2.js 将 m_rgAssets 初始化为数组并按 assetid 写入；其 Prototype 1.7 覆盖 Object.values，使用未检查自有属性的 for-in 遍历，会把数组继承方法作为返回值。
- 使用从 Steam 官方 CDN 下载的完整 Prototype 脚本在 jsdom 中复现：2 个自有资产得到 37 个 Object.values 返回项，v0.1.2 精确报出“原生交接失败（校验价格与库存）：Steam 原生库存描述缺失”。
- 改为 for-in 配合 Object.prototype.hasOwnProperty.call，仅遍历自有资产。相同完整 Prototype 环境下新版返回 filled，数量为 3 / 2、首行买方价 11.44，出售调用次数为 0。
- 新增 Prototype 枚举回归和真实资产缺失描述仍拒绝交接的测试；共 41 项通过。未自动加载用户安装的扩展或执行真实上架。
- 公开源： https://community.fastly.steamstatic.com/public/javascript/prototype-1.7.js?v=npJElBnrEO6W&l=schinese&_cdn=fastly 。下载文件只在 work 中用于复现，不打包入扩展。
